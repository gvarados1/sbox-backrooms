using Sandbox;
using System;
using System.Linq;

namespace ProcGen;

partial class ProcModel : ModelEntity
{
  // We shouldn't be spawning a entitiy for each piece of wall.
  // Can we spawn models without entities? Can we merge all the walls together with code?
}
